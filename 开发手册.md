# 开发手册

## 项目基础

### 创建与初始化
```shell
git clone git@github.com:gemiman/daoyi-vue-rs.git
cargo init
```

### 运行环境配置

项目通过环境变量或 `application.yaml` 进行配置。

- **运行模式**: `ACTIVE_PROFILE=local` (对应加载 `application-local.yaml`)
- **端口设置**: `DY__SERVER__PORT=8080` (环境变量优先级最高)
- **SQL 日志**: `RUST_LOG=DEBUG` 可查看 SeaORM 生成的 SQL 语句。

**运行示例：**
```shell
ACTIVE_PROFILE=local DY__SERVER__PORT=8080 cargo run --bin daoyi-demo
```

---

## 数据库开发 (SeaORM)

### 安装与生成 Entity
```shell
cargo install sea-orm-cli
```

使用 `sea-orm-cli` 从数据库表生成代码：
```shell
sea-orm-cli generate entity -s system --with-serde both --model-extra-attributes 'serde(rename_all="camelCase")' --date-time-crate chrono -o ./crates/libs/daoyi-entity-system/src/system_entity
```

```shell
sea-orm-cli generate entity -s system --with-serde both --model-extra-attributes 'serde(rename_all="camelCase")' --date-time-crate chrono -o ./docs/sea-orm-entities/system
```

```shell
sea-orm-cli generate entity -s cti --with-serde both --model-extra-attributes 'serde(rename_all="camelCase")' --date-time-crate chrono -o ./docs/sea-orm-entities/cti
```

### 事务管理 (`#[transactional]`)

项目实现了类 Spring 的声明式事务。

- **使用方法**: 在 Service 方法上添加 `#[transactional]`。
- **注意事项**:
    1. 必须使用 `daoyi_common_support::database::get().await` 获取数据库连接。
    2. 在宏方法内部，`db` 是一个 `DbGuard` 值类型。调用 SeaORM 方法（如 `insert`, `find`）时，**必须使用引用 `&db`**。

```rust
use daoyi_macros::transactional;
use daoyi_common_support::database;

#[transactional]
pub async fn create_user(vo: UserSaveReqVO) -> ApiResult<String> {
    let db = database::get().await; // 自动获取当前事务或开启新事务

    // 注意：必须传 &db (引用)
    let user = User::insert(&db, vo).await?;

    Ok(user.id)
}
```

### 模型增强 (`#[daoyi_model]`)

在 Entity 定义中使用 `#[daoyi_model]` 宏，可以获得以下功能：

1. **自动审计**: 自动处理 `create_time`, `update_time`, `creator`, `updater`, `deleted`, `tenant_id` 等字段。
2. **自动 ID**: 自动生成分布式 ID（如 Snowflake）。
3. **软删除**: 自动集成软删除逻辑。

---

## Web API 开发 (Axum)

### 常用提取器与返回类型

项目封装了标准的响应格式和校验机制。

- **返回类型**: `RestApiResult<T>` (即 `Result<ApiResponse<T>, ApiError>`)。
- **JSON 校验**: `ValidJson<T>` 自动进行业务字段校验（基于 `validator` 库）。
- **Query 校验**: `ValidQuery<T>` 处理 GET 请求参数。

```rust
#[debug_handler]
pub async fn create_notice(
    State(state): State<AppState>,
    ValidJson(vo): ValidJson<NoticeSaveReqVO>
) -> RestApiResult<String> {
    let id = system_notice_service::create_notice(vo).await?.id;
    ApiResponse::success(id)
}
```

---

## WebSocket / 消息队列 (MQ) 开发

项目通过 WebSocket 配合 Redis Pub/Sub 实现了跨集群的消息广播。

### 1. 添加新的消息监听器 (Listener)

1. **定义消息结构**: 定义 `MyReq` (Deserialize) 和 `MyResp` (Serialize)。
2. **实现 Trait**: 实现 `WebSocketMessageListener` 并指定 `get_type()`。
3. **注册**: 在模块初始化时调用 `ws_manager.add_listener()`。

### 2. 服务端主动推送消息

通过 `AppState` 中的 `ws_sender` 进行推送，支持集群广播：

```rust
// 推送给指定类型的全体在线用户 (1: Admin, 2: Member)
state.ws_sender.send_by_user_type(1, "notice-push", notice_vo).await;

// 推送给指定 ID 的在线用户
state.ws_sender.send_by_user(1, "1024", "order-update", order_vo).await;
```

---

## 文件存储系统

项目支持多种文件存储协议，可通过配置无缝切换。

- **支持协议**: `Local`, `S3` (Aliyun, MinIO), `FTP`, `SFTP`, `Database`。
- **使用方法**: 通过 `daoyi-entity-infra` 中的 `FileClientFactory` 创建客户端。